# C5｜Chips, Compute, Data, Terminals, and Industrial Infrastructure

If evaluation results are expected to be reused across organizations, quality evaluation becomes shared infrastructure rather than a local project script

The infrastructure problem expands to:

```text
standardized indicators
→ evaluation datasets
→ evaluation tools / systems
→ result packaging
→ mutual-recognition workflow
→ re-evaluation / revocation
```

**Open question:** whether public pilots expose enough metadata to reproduce or independently audit an evaluation result
